{% extends 'base.html.twig' %}

{% block title %}Новая категория{% endblock %}

{% block body %}
    <h1>Добавить категорию</h1>

    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.isIncome) }}
    <button type="submit" class="btn">Сохранить</button>
    {{ form_end(form) }}

    <a href="{{ path('category_index') }}">Назад</a>
{% endblock %}